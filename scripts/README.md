# Scripts

Shared deployment and interaction scripts can live here. The Hardhat deployment entrypoint is currently in `contracts/scripts/deploy.ts` so it can run directly through Hardhat:

```bash
cd ../contracts
npm run deploy:arc
```
