# Only exclude source-side junk from upload. Do NOT ignore `.next` —
# newer Vercel build pipelines can apply ignore rules after `next build`
# and then fail with: lstat '.../.next/package.json' ENOENT.
node_modules
.next-dev
out
dist
.env
.env.*
!.env.example
*.log
.chrome-*
.edge-*
